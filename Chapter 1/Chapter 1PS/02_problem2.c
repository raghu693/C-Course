#include <stdio.h>

int main(){
    float r = 5, h = 12;
    float PI = 3.14;
    printf("The Area of the circle is : %f\n", PI*(r*r));
    printf("The Volume of the Cylinder is : %f\n", PI*(r*r)*h);
    return 0;
}