#include <stdio.h>

int main(){
    // Quick Quiz: Write a program to print first ‘n’ natural number using do-while loop.

    int n = 1;
    int i = 34;

    do{
        /* code */
        printf("%d\n", n);
        n++;
    } while (n <= i);
    
    return 0;
}