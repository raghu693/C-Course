#include <stdio.h>

int main(){
    int i = 1;
    while (i <= 20){
        /* code */
        if (i >= 10){
            /* code */
            printf("The Value of i is %d\n", i);
        }
            i++;
    }
    
    return 0;
}